菜单
1.添加是否外链接属性，可使用hash路由时可跳出#链接，相当于外链
2.设置权限标识为当前路由，默认为列表即xxx,增加编辑删除统一在后面添加:add/edit/del，除此还需支持自定增加
3.增加类型以支持一键创建表单
4.list删除有before但没有after事件
5.大屏表格数据将表头及列数据合并，支持从接口统一返回
6.大屏表格改为普通表格以方便样式调整

大屏
1.添加点击事件
2.添加div辅助组件
3.绑定鼠标事件导致输入框有问题，有时焦点在left却改变了top的值
4.添加文本时，对文本添加样式，内还有一个div不方便写样式，添加多一个样式
5.自定义组件将宽高作为props传参
6.文本组件时改为文本框
7.输入改变时添加防抖，不用每次输入都重新渲染
8.点击组件对应图层没有选中，点击图层也没选中对应组件

组件子表如何支持在修改时可以继续添加，但不能删除已有的

大屏修改动态api时每次输入都会触发图表重新渲染

待优化：
1.设计表单设置了初始值时，当使用了重置表单时会清空设置的初始值
2.标题添加对齐方式，其他属性应该改为css方便对字体大小颜色等处理
3.添加表格布局
4.布局字段格栅布局选中单元格时字段配置有问题
5.退出时没有清空字典，因此重新登录没有重新加载

问题：
下拉返回时显示了value
