菜单
1.添加是否外链接属性，可使用hash路由时可跳出#链接，相当于外链
2.设置权限标识为当前路由，默认为列表即xxx,增加编辑删除统一在后面添加:add/edit/del，除此还需支持自定增加
3.增加类型以支持一键创建表单
4.list删除有before但没有after事件
5.大屏表格数据将表头及列数据合并，支持从接口统一返回
6.大屏表格改为普通表格以方便样式调整

大屏
1.添加点击事件

组件子表如何支持在修改时可以继续添加，但不能删除已有的

问题：
下拉返回时显示了value